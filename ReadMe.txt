To check URL validity run the python server first:
%python3 url_vaidator.py

Make sure that these packages are installed first:

pip install flask requests
pip install -U flask-cors


open index.html in any browser